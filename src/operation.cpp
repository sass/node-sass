#include "operation.hpp"

#ifndef SASS_AST
#include "ast.hpp"
#endif

namespace Sass {

  // template <typename T>
  // T Operation<T>::operator()(AST_Node*)                 { return T(); }

  // // statements
  // template <typename T>
  // T Operation<T>::operator()(Block*)                    { return T(); }

  // template <typename T>
  // T Operation<T>::operator()(Ruleset*)                  { return T(); }

  // template <typename T>
  // T Operation<T>::operator()(Propset*)                  { return T(); }

  // template <typename T>
  // T Operation<T>::operator()(Media_Block*)              { return T(); }

  // template <typename T>
  // T Operation<T>::operator()(At_Rule*)                  { return T(); }

  // template <typename T>
  // T Operation<T>::operator()(Declaration*)              { return T(); }

  // template <typename T>
  // T Operation<T>::operator()(Assignment*)               { return T(); }

  // template <typename T>
  // T Operation<T>::operator()(Import*)                   { return T(); }

  // template <typename T>
  // T Operation<T>::operator()(Import_Stub*)              { return T(); }

  // template <typename T>
  // T Operation<T>::operator()(Warning*)                  { return T(); }

  // template <typename T>
  // T Operation<T>::operator()(Comment*)                  { return T(); }

  // template <typename T>
  // T Operation<T>::operator()(If*)                       { return T(); }

  // template <typename T>
  // T Operation<T>::operator()(For*)                      { return T(); }

  // template <typename T>
  // T Operation<T>::operator()(Each*)                     { return T(); }

  // template <typename T>
  // T Operation<T>::operator()(While*)                    { return T(); }

  // template <typename T>
  // T Operation<T>::operator()(Return*)                   { return T(); }

  // template <typename T>
  // T Operation<T>::operator()(Content*)                  { return T(); }

  // template <typename T>
  // T Operation<T>::operator()(Extend*)                   { return T(); }

  // template <typename T>
  // T Operation<T>::operator()(Definition*)               { return T(); }

  // template <typename T>
  // T Operation<T>::operator()(Mixin_Call*)               { return T(); }

  //   // expressions
  // template <typename T>
  // T Operation<T>::operator()(List*)                     { return T(); }

  // template <typename T>
  // T Operation<T>::operator()(Binary_Expression*)        { return T(); }

  // template <typename T>
  // T Operation<T>::operator()(Unary_Expression*)         { return T(); }

  // template <typename T>
  // T Operation<T>::operator()(Function_Call*)            { return T(); }

  // template <typename T>
  // T Operation<T>::operator()(Variable*)                 { return T(); }

  // template <typename T>
  // T Operation<T>::operator()(Textual*)                  { return T(); }

  // template <typename T>
  // T Operation<T>::operator()(Number*)                   { return T(); }

  // template <typename T>
  // T Operation<T>::operator()(Percentage*)               { return T(); }

  // template <typename T>
  // T Operation<T>::operator()(Dimension*)                { return T(); }

  // template <typename T>
  // T Operation<T>::operator()(Color*)                    { return T(); }

  // template <typename T>
  // T Operation<T>::operator()(Boolean*)                  { return T(); }

  // template <typename T>
  // T Operation<T>::operator()(String_Schema*)            { return T(); }

  // template <typename T>
  // T Operation<T>::operator()(String_Constant*)          { return T(); }

  // template <typename T>
  // T Operation<T>::operator()(Media_Query_Expression*)   { return T(); }

  //   // parameters and arguments
  // template <typename T>
  // T Operation<T>::operator()(Parameter*)                { return T(); }

  // template <typename T>
  // T Operation<T>::operator()(Parameters*)               { return T(); }

  // template <typename T>
  // T Operation<T>::operator()(Argument*)                 { return T(); }

  // template <typename T>
  // T Operation<T>::operator()(Arguments*)                { return T(); }

  //   // selectors
  // template <typename T>
  // T Operation<T>::operator()(Selector_Schema*)          { return T(); }

  // template <typename T>
  // T Operation<T>::operator()(Selector_Reference*)       { return T(); }

  // template <typename T>
  // T Operation<T>::operator()(Selector_Placeholder*)     { return T(); }

  // template <typename T>
  // T Operation<T>::operator()(Type_Selector*)            { return T(); }

  // template <typename T>
  // T Operation<T>::operator()(Selector_Qualifier*)       { return T(); }

  // template <typename T>
  // T Operation<T>::operator()(Attribute_Selector*)       { return T(); }

  // template <typename T>
  // T Operation<T>::operator()(Pseudo_Selector*)          { return T(); }

  // template <typename T>
  // T Operation<T>::operator()(Negated_Selector*)         { return T(); }

  // template <typename T>
  // T Operation<T>::operator()(Simple_Selector_Sequence*) { return T(); }

  // template <typename T>
  // T Operation<T>::operator()(Selector_Combination*)     { return T(); }

  // template <typename T>
  // T Operation<T>::operator()(Selector_Group*)           { return T(); }


}