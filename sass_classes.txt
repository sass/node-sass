node ( string, size_t, etc )

    statement
    value
    selector
    parameter
    argument


statement

    block           ( statement[] )
    ruleset         ( selector, block )
    propset         ( identifier, block )
    media_query     ( value, block )
    directive       ( identifier, value )
    block_directive ( identifier, selector, block )
    
    rule            ( identifier, value[] )
    assignment      ( identifier, value, guarded? )
    import          ( text )
    warning         ( text )
    comment         ( text )

    if_directive    ( value[], block[] )
    for_directive   ( identifier, value, value, block, inclusive? )
    each_directive  ( identifier value, block )
    while_directive ( value, block )

    extend          ( selector )

    mixin_def       ( mixin )
    mixin_call      ( identifier, argument[], block )

    function_def    ( function )


value ( parenthesized? )

    list                ( value[], comma_separated? )
    disjunction         ( value[] )
    conjunction         ( value[] )
    relation            ( rel_op, value, value )
    expression          ( add_op, value, value )
    term                ( mul_op, value, value )
    textual_percentage  ( text )
    textual_dimension   ( text )
    textual_number      ( text )
    textual_hex         ( text )
    percentage          ( double )
    dimension           ( double, text )
    number              ( double )
    color               ( double, double, double, double )
    identifier          ( text[] )
    string              ( text[] )
    unary               ( un_op, value )
    function_call       ( identifier, argument[] )


selector

    group       ( combination[] )
    combination ( combinator, sequence, combination )
    sequence    ( simple[] )
    simple      ( text )


parameter ( identifier, value, default_value?, rest? )

argument ( value, identifier, keyword_argument?, rest? )





















